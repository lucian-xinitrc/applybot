name: Restart Service

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Run commands on remote server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            cd /home/lucian/applybot
            git pull
            sudo docker stop apply-bot
            sudo docker rm apply-bot
            sudo docker build -t applybot .
            sudo docker run -d --restart unless-stopped --name apply-bot applybot